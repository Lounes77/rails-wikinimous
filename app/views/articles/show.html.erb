<h1><%= @article.title %>

<%= link_to "🖍", edit_article_path(@article) %>

<%= link_to "🗑", article_path(@article), data: {turbo_method: :delete,
turbo_confirm: 'Are Your sure?' }  %>
</h1>

<p> <%= @article.content %></p>
